import { BtnHilightDirective } from './btn-hilight.directive';

describe('BtnHilightDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnHilightDirective();
    expect(directive).toBeTruthy();
  });
});
